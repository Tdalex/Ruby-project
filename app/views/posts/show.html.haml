= render 'post', post: @post
- if @post.user.id == current_user.id
  .text-center.edit-links
    = link_to "Annuler", posts_path
    |
    = link_to "Modifier Post", edit_post_path(@post)
- else
  .text-center.edit-links
    =link_to "Annuler", posts_path